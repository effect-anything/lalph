---
"@effect-x/lalph": patch
---

Inject `LALPH_*` hook context into worktree command helpers and the legacy worktree setup script so hooks and setup commands can access repository and workspace metadata consistently.
