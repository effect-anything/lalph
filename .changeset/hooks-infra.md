---
"lalph": patch
---

Add the initial hooks configuration infrastructure, including schema validation for `.lalph/hooks.yml`, YAML-backed config loading, and template interpolation helpers for upcoming worktree hook execution.
